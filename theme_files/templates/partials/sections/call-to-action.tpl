{if $cta}
    <!-- Optional Call to Action ======================================= -->
    <section class="call-to-action">

        <div class="content-container">

            <a href="{$cta.link}" title="{$cta.text}" target="{$cta.target}" class="button--blue call-to-action__button">{$cta.text}</a>

        </div>

    </section>
{/if}
